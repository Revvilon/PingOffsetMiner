package pom.rewrite.features;

import pom.rewrite.config.Feature;
import pom.rewrite.config.settings.SettingBoolean;

public class PingOffsetMiner {
    public static final Feature instance = new Feature("pomEnabled", true);
}
